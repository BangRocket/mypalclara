[build]
dockerfilePath = "Dockerfile.discord"

[deploy]
# No healthcheckPath - Discord bot doesn't serve HTTP
startCommand = "python discord_crewai.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
