---
phase: 03-cli-client-retirement
plan: 03
type: execute
wave: 2
depends_on:
  - "03-02"
files_modified:
  - docker-compose.yml
  - CLAUDE.md
  - README.md
autonomous: true

must_haves:
  truths:
    - "discord_bot.py no longer exists in repository"
    - "email_monitor.py no longer exists in repository"
    - "python -m gateway starts all providers"
    - "docker-compose has single gateway service for all providers"
    - "Documentation reflects new architecture"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Updated container configuration"
      contains: "gateway"
    - path: "CLAUDE.md"
      provides: "Updated development documentation"
      contains: "python -m gateway"
    - path: "README.md"
      provides: "Updated project documentation"
      contains: "gateway"
  key_links:
    - from: "docker-compose.yml"
      to: "gateway service"
      via: "discord-bot service replaced/deprecated"
      pattern: "gateway:"
---

<objective>
Delete legacy files and update documentation for the gateway-only architecture.

Purpose: Complete the migration to gateway architecture by removing obsolete entry points and ensuring all documentation reflects the new single entry point.

Output: discord_bot.py and email_monitor.py deleted, documentation updated, Docker Compose simplified.
</objective>

<execution_context>
@/Users/heidornj/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heidornj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-cli-client-retirement/03-RESEARCH.md
@.planning/phases/03-cli-client-retirement/03-02-SUMMARY.md
@docker-compose.yml
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete legacy files</name>
  <files>discord_bot.py, email_monitor.py, discord_monitor.py</files>
  <action>
PREREQUISITE: Plan 03-02 SUMMARY must show DELETION STATUS: SAFE

Delete the legacy entry points that have been migrated to gateway/providers/:

```bash
git rm discord_bot.py
git rm email_monitor.py
git rm discord_monitor.py  # If exists, also delete the monitor dashboard
```

These files have been replaced by:
- discord_bot.py (4384 lines) -> gateway/providers/discord.py
- email_monitor.py (803 lines) -> gateway/providers/email.py
- discord_monitor.py -> (monitoring now in gateway health checks)

After deletion, verify no broken imports:
```bash
python -c "from gateway.main import main; print('OK')"
python -c "from adapters.cli.main import run; print('OK')"
```

DO NOT delete cli_bot.py - it's now a migration wrapper (from Plan 01).
  </action>
  <verify>
`ls discord_bot.py email_monitor.py 2>&1` returns "No such file or directory" for both.
`python -c "from gateway.main import main"` succeeds.
  </verify>
  <done>Legacy entry points deleted from repository with git rm</done>
</task>

<task type="auto">
  <name>Task 2: Update Docker Compose</name>
  <files>docker-compose.yml</files>
  <action>
Update docker-compose.yml to consolidate services:

1. Find the `discord-bot` service section
2. Either:
   - OPTION A (Recommended): Mark as deprecated with comment, keep for transitional users
   - OPTION B: Remove entirely and update gateway service to include Discord env vars

For OPTION A, add comment to discord-bot service:
```yaml
  # DEPRECATED: Use gateway service instead
  # Discord functionality is now built into the gateway
  # This service will be removed in a future release
  discord-bot:
    ...
```

For gateway service, ensure it has:
- All Discord-related env vars (DISCORD_BOT_TOKEN, etc.)
- All Email-related env vars (EMAIL_MONITORING_ENABLED, etc.)
- Comment explaining it runs all providers

Add to gateway service environment (if not present):
```yaml
      # Discord (moved from discord-bot service)
      - DISCORD_BOT_TOKEN=${DISCORD_BOT_TOKEN:-}
      - DISCORD_CLIENT_ID=${DISCORD_CLIENT_ID:-}
      - DISCORD_ALLOWED_SERVERS=${DISCORD_ALLOWED_SERVERS:-}
      - DISCORD_ALLOWED_CHANNELS=${DISCORD_ALLOWED_CHANNELS:-}
      # Email monitoring
      - EMAIL_MONITORING_ENABLED=${EMAIL_MONITORING_ENABLED:-false}
```

Update gateway service description comment:
```yaml
  # Clara Gateway - Single entry point for all providers (Discord, Email, CLI)
  gateway:
```
  </action>
  <verify>
`grep -A2 "Clara Gateway" docker-compose.yml` shows updated description.
Gateway service has Discord env vars.
  </verify>
  <done>docker-compose.yml updated with gateway as primary service, discord-bot deprecated</done>
</task>

<task type="auto">
  <name>Task 3: Update CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <action>
Update CLAUDE.md to reflect the gateway architecture:

1. Find "Development Commands" section
2. Remove or update references to:
   - `poetry run python discord_bot.py` -> use gateway
   - `poetry run python email_monitor.py` -> integrated into gateway
   - Any standalone discord_bot/email_monitor commands

3. Add/update Gateway section:
```markdown
### Gateway (All Providers)
```bash
poetry run python -m gateway              # Start gateway with all providers
poetry run python -m gateway --help       # Show options
```

The gateway runs Discord, Email, and CLI providers in a single process.
```

4. Update "CLI" section to reference new command:
```markdown
### CLI
```bash
poetry run clara-cli                      # Connect to gateway (recommended)
poetry run python -m adapters.cli         # Alternative
poetry run python cli_bot.py              # Deprecated, redirects to above
```
```

5. Update Docker section:
```markdown
### Docker
```bash
docker-compose --profile gateway up       # Run gateway with all providers
```
```

6. Update "Architecture" section if needed to mention gateway/providers structure.
  </action>
  <verify>
`grep "python -m gateway" CLAUDE.md` returns matches.
`grep "discord_bot.py" CLAUDE.md` returns only deprecation notices or removal notes.
  </verify>
  <done>CLAUDE.md updated with gateway commands, deprecated references removed</done>
</task>

</tasks>

<verification>
- [ ] discord_bot.py deleted (not in repo)
- [ ] email_monitor.py deleted (not in repo)
- [ ] `python -m gateway` imports successfully
- [ ] docker-compose.yml has gateway as primary service
- [ ] discord-bot service marked deprecated or removed
- [ ] CLAUDE.md documents `python -m gateway` as primary command
- [ ] CLAUDE.md deprecates old entry points
- [ ] No import errors after deletion
</verification>

<success_criteria>
1. `git status` shows discord_bot.py and email_monitor.py as deleted
2. `docker-compose --profile gateway config` validates successfully
3. CLAUDE.md documents the gateway architecture
4. All Python imports still work after deletion
5. Clear migration path documented for users
</success_criteria>

<output>
After completion, create `.planning/phases/03-cli-client-retirement/03-03-SUMMARY.md`
</output>
