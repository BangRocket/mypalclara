---
phase: 03-cli-client-retirement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cli_bot.py
  - pyproject.toml
autonomous: true

must_haves:
  truths:
    - "poetry run python cli_bot.py shows migration notice and starts gateway CLI"
    - "poetry run python -m adapters.cli connects to gateway and works interactively"
    - "CLI history preserved at ~/.clara_cli_history"
  artifacts:
    - path: "cli_bot.py"
      provides: "Migration wrapper that redirects to adapters.cli"
      contains: "adapters.cli.main"
    - path: "pyproject.toml"
      provides: "clara-cli script entry point"
      contains: "[tool.poetry.scripts]"
  key_links:
    - from: "cli_bot.py"
      to: "adapters/cli/main.py"
      via: "import and call run()"
      pattern: "from adapters.cli.main import run"
---

<objective>
Refactor cli_bot.py to be a thin wrapper that redirects to the gateway-based CLI client.

Purpose: Preserve backward compatibility for users who run `python cli_bot.py` while transitioning to the new gateway architecture.

Output: cli_bot.py becomes a migration stub, pyproject.toml gains a clara-cli script entry point.
</objective>

<execution_context>
@/Users/heidornj/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heidornj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-cli-client-retirement/03-RESEARCH.md
@adapters/cli/main.py
@adapters/cli/gateway_client.py
@cli_bot.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert cli_bot.py to migration wrapper</name>
  <files>cli_bot.py</files>
  <action>
Replace the entire cli_bot.py (621 lines) with a thin migration wrapper:

1. Keep the docstring but update it to reference the new command
2. Import from adapters.cli.main import run
3. Print a deprecation notice explaining the migration:
   - "[yellow]Note: cli_bot.py is deprecated.[/yellow]"
   - "Use: poetry run python -m adapters.cli"
   - "Or: poetry run clara-cli"
4. Call run() to actually start the CLI

The file should be approximately 30-40 lines total. Use Rich for colored output.

This preserves backward compatibility - old invocations still work, but users see the migration path.
  </action>
  <verify>
Run: `poetry run python cli_bot.py --help 2>&1 | head -5`
Should show the deprecation notice before starting.
  </verify>
  <done>cli_bot.py shows deprecation notice and delegates to adapters.cli.main.run()</done>
</task>

<task type="auto">
  <name>Task 2: Add clara-cli script to pyproject.toml</name>
  <files>pyproject.toml</files>
  <action>
Add a scripts entry point under [tool.poetry.scripts]:

```toml
[tool.poetry.scripts]
clara-cli = "adapters.cli.main:run"
```

This allows users to run `poetry run clara-cli` as the canonical way to start the CLI.

If the [tool.poetry.scripts] section doesn't exist, create it after [tool.poetry.dependencies] or the appropriate location.
  </action>
  <verify>
Run: `grep -A2 "\[tool.poetry.scripts\]" pyproject.toml`
Should show the clara-cli entry.
  </verify>
  <done>pyproject.toml has clara-cli script pointing to adapters.cli.main:run</done>
</task>

<task type="auto">
  <name>Task 3: Verify CLI client works with gateway</name>
  <files></files>
  <action>
Test that the adapters.cli module works correctly:

1. Check that the module can be imported without errors:
   `python -c "from adapters.cli.main import run; print('OK')"`

2. Check the gateway client can be imported:
   `python -c "from adapters.cli.gateway_client import CLIGatewayClient; print('OK')"`

3. Verify cli_bot.py migration wrapper works:
   `timeout 2 poetry run python cli_bot.py || true`
   (Will fail to connect to gateway but should show deprecation notice)

Note: Full integration testing requires a running gateway, which is covered in Plan 02.
  </action>
  <verify>
All import checks pass. cli_bot.py shows deprecation message before attempting gateway connection.
  </verify>
  <done>CLI modules import successfully, migration wrapper shows deprecation notice</done>
</task>

</tasks>

<verification>
- [ ] cli_bot.py is now a thin wrapper (~30-40 lines)
- [ ] Deprecation notice printed when running cli_bot.py
- [ ] pyproject.toml has clara-cli script entry
- [ ] `poetry run clara-cli` command is available (after poetry install)
- [ ] adapters.cli modules import without errors
</verification>

<success_criteria>
1. `poetry run python cli_bot.py` shows deprecation notice and delegates to gateway CLI
2. `poetry run clara-cli` is the new canonical command (in pyproject.toml)
3. No breaking changes to existing CLI behavior (just added notice)
4. CLI client code at adapters/cli/ unchanged (already works)
</success_criteria>

<output>
After completion, create `.planning/phases/03-cli-client-retirement/03-01-SUMMARY.md`
</output>
