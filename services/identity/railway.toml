[build]
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python -m identity.main"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
