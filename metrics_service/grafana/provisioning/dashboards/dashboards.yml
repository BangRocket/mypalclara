# Grafana dashboard provisioning
apiVersion: 1

providers:
  - name: "Clara Dashboards"
    orgId: 1
    folder: "Clara"
    folderUid: "clara"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
